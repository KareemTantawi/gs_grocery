import 'package:flutter/material.dart';

class GcGrocery extends StatelessWidget {
  const GcGrocery({super.key});

  @override
  Widget build(BuildContext context) {
    return const MaterialApp(
      debugShowCheckedModeBanner: false,
      home: Scaffold(),
    );
  }
}
